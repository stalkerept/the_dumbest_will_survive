$||y|| = ||Ax|| \leq k ||x||$
Введём $||A|| := \inf{k}$ (проверять не нужно, т.к. это не потребуется). Работает скорее как $оценка$
Обозначим базис как $\{e\}$, тогда:
$y_{e, \downarrow} = A_{e}x_{e, \downarrow}$
$||y||_{2} = \left( \sum_{j=1}^{n}{|y_{j}|^{2}} \right)^{\frac{1}{2}}$
$$
\begin{bmatrix}
     y_{1}\\
    y_{2} \\
	\dots \\
	y_{j} \\
	\dots \\
	y_{n}
\end{bmatrix} =

\begin{bmatrix}
    a_{11} & \dots a_{1n} \\
    a_{j1} & \dots a_{jn} \\
	\dots \\
	a_{n1} &\dots a_{nn}
\end{bmatrix}
\begin{bmatrix}
x_{1} \\
\dots \\
x_{n}
\end{bmatrix}
$$
$$
\begin{align*}
||y||_{2} = \left( \sum_{j=1}^{n}{|y_{j}|^{2}} \right)^{\frac{1}{2}} = \left( \sum_{j=1}^{n}{|\sum_{k=1}^{n}{a_{jk}x_{k}}|^{2}} \right)^{\frac{1}{2}} \leq (\text{К-Б}) \sum_{j=1}^{n}\left( \sum_{k=1}^{n}{|a_{jk}|^{2}} \sum_{k=1}^{n}{|x_{k}|^{2}} \right)^{\frac{1}{2}} = \left( \sum_{j,k = 1}^{n}{|a_{jk}|^{2}} \right)^{\frac{1}{2}} \cdot \left( \sum_{k = 1}^{n}{|x_{k}|^{2}} \right)^{\frac{1}{2}} \\
\text{, где } \left( \sum_{k = 1}^{n}{|x_{k}|^{2}} \right)^{\frac{1}{2}} = ||x||_{2} \text{ и оно не зависит от j, и где } \left( \sum_{j,k = 1}^{n}{|a_{jk}|^{2}} \right)^{\frac{1}{2}} - \text{оптимальное значение k}
\end{align*}
$$
$$
\begin{align*}
y(t)_{\downarrow} = A(t)x_{\downarrow} \\
||y(t) - y(t)||_{2} \leq \left( \sum_{j, k = 1}^{n}{|a_{jk}(t) - a_{jk}(t)|^{2}} \right)^{\frac{1}{2}} ||x_{2}||
\end{align*}
$$
### Метрические пространства
#### Определение:
$$
\begin{align*}
\forall{x, y} \in X \to d(x, y) \\
1)d(x, y) \geq  0, \quad d(x, y) = 0 \leftrightarrow  x= y \\
2)d(x, y) = d(y, x) \\
3) d(x, z) \leq  d(x, y) + d(y, z)
\end{align*}
$$
Пример того, где 2 свойство не выполняется. В горах мерят расстояние во времени. Расстояние между точками во времени. И получается снизу вверх медленнее, чем сверху вниз.

$$
\begin{align*}
X = \{\mathbb{R}^{n} (\mathbb{C}^{n}), \quad d(x, y) = \sqrt{ \sum_{1}^{n}{|x_{j}-y_{j}|^{2}} }\} \text{ (Определение метрического пространства)}\\ 
B(a, \gamma) : = \{x\in \mathbb{R}^{n}: d(a, x)<\gamma\} \text{(Определение открытого шара)}\\
G \subset \mathbb{R}^{n} \text{ открыто, если } \forall{x} \in G \quad \exists{B(x, \gamma)} < G
\end{align*}
$$
#### Примеры:
1) $\mathbb{R}^{n}$ открыто
2) $\not O$ открыто
Отрицание $A$ не открыто, если $\exists{x\in A}: \quad \forall{B(x, \gamma) \not\subset A}$
3) Открытый шар открыт (граница не входит). 
![[Pasted image 20260429092559.png]]$$
\begin{align*}
\forall{x, \xi \in B(x, \gamma - d(a, x))} \quad d(\xi, a) \leq d(x, \xi) + d(x, a) < (\gamma - d(a, x)) + d(x, a) = \gamma
\end{align*}
$$
#### Определение 4
$$
F \subset \mathbb{R}^{n} \text{ замкнуто, если } \mathbb{R}^{n}\F \text{открыто}
$$
#### Определение 5
$\overline{B(a, \gamma)}$ замкнутый шар, где $\overline{B(a, \gamma)} := \{x \in \mathbb{R}^{n}: d(x, a)\leq \gamma\}$ (граница входит)

#### Примеры
4) $\not O$ и $\mathbb{R}^{n}$ замкнуты. $\mathbb{R}^{n} \ \not O = \mathbb{R}^{n}$ открыто, $\mathbb{R}^{n}\ \mathbb{R}^{n} = \not O$ открыто
5) $\overline{B(a, \gamma)}$ замкнут
![[Pasted image 20260429093130.png]]
$x \in \mathbb{R}^{n} \ \overline{B(a, \gamma)}$, т.е. $d(a, x) > \gamma \quad \forall{\xi\in B(x, d(a, x)-\gamma)}$
$d(a, x)\leq d(a, \xi) + d(\xi, x) \implies d(a, \xi) \geq d(a, x) - d(x, \xi) > d(a, x) - (d(a, x) - \gamma) = \gamma$

#### Утверждение 1
$G_{\alpha} \in A$, открытые множества. Тогда выполняются следующее:
1. $U_{\alpha} G_{\alpha}$ открыто
2. $\cap_{k=1}^{m}{G}_{k}$ открыто
##### **Доказательство**:
1. $x \in U_{\alpha}G_{\alpha} \implies \exists{\alpha_{x}}: x\in G_{\alpha x}$. Т.к. $G_{a x}$ открыто, то $\exists{B(x, \gamma_{x})} \subset G_{\alpha x} \subset U_{\alpha}G_{\alpha}$
2. $x \in \cap_{k=1}^{m}{G_{k}} \quad \forall{k=1,\dots ,m} \exists{\gamma_{k}>0}: B(x, \gamma_{k}) \subset G_{k}$
$\gamma := \min_{k = 1, \dots, m}{\gamma_{k}>0}$
$B(x, \gamma) \subset B(x, \gamma_{k}) \subset G_{k} \implies B(x, \gamma) \subset \cap_{k}G_{k}$
##### Пример:
$[-1; 1] = \cap_{k=1}^{\infty}{\left( -1 - \frac{1}{2^{n}}; 1+ \frac{1}{2^{n}} \right)}$

#### Утверждение 2:
$F_{\alpha} \text{ где } \alpha \in A$ замкнуты.
1. $\cap_{\alpha} F_{\alpha}$ замкнуто
2. $\cup_{k=1}^{m}{F_{k}}$ замкнуто
$\mathbb{R}^{n}\(\cap_{\alpha} F_{\alpha}) = \cup_{k=1}^{m}{\mathbb{R}^{n}\F_{k}}$
Левая часть открыто по утверждению 1.
##### Примеры:
$(-1; 1) = \cup{\left[ -1 + \frac{1}{2^{n}}; 1-\frac{1}{2^{n}} \right]}$


$S(a, \gamma) := \{x \in \mathbb{R}^{n}: d(x, a) = \gamma\}$
Это сфера, она замкнута. С центром в точке $a$ с радиусом $\gamma$
$S(a, \gamma) = \overline{B(a, \gamma)} \cap \mathbb{R}^{n}\B(a,\gamma)$ замкнуто (определение)

Немного про окрестности и так далее

Зачем мы вообще это делаем?
Мы хотим определять непрерывность, а для этого нужно определять расстояние между точками.

#### Определение 6:
Открытое множество, содержащее точку $a \to$ окрестность $a, U_{a}$
#### Определение 7:
Имеется множество точек $E \subset \mathbb{R}^{n} \quad x\in \mathbb{R}^{n}, \text{ если}$:
1. $\exists{U_{x}\subset E} \implies x$ - внутренняя точка $E$
2. $\exists{U_{x}\subset \mathbb{R}_{n}\E} \implies x$ - внешняя точка $E$
3. $\forall{U_{x}}$ есть точки из $E$ и из $\mathbb{R}^{n}\E \implies$ x - граничная точка $E$
#### Определение 8:
Объединение граничных точек $E$ - граница $E$, $\partial E$
#### Пример 7:
$S(a, \gamma) = \partial B(a, \gamma)$; $S(a, \gamma) = \partial \overline{B(a, \gamma)}$; $S(a, \gamma) = \partial S(a, \gamma)$
1. $S(a, \gamma) = \partial B(a, \gamma) \quad \forall{x} \in S(a, \gamma) \quad \forall{B(x, \rho)}$